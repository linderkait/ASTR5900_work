import numpy as np
import matplotlib.pyplot as plt

#Problem 1
#Part b
def DFT(f, N):
    #the function takes a function f(x) and the number of steps to use N and outputs the 
    #Fourier transform F(k)



def iDFT(F, N):
    #the function takes a function F(k) and the number of steps N and outputs the 
    #Fourier transform f(x)
    